import { redirect } from "next/navigation"
import { createClient } from "@/utils/supabase/server"
export const metadata = { title: "Community — Career Copilot" }
const THREADS = [{ id: "todo-1", title: "Welcome to exam spaces", flair: "Discussion", preview: "Community schema is being enabled in production. This is a visual preview shell." }]
export default async function CommunityPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/auth/login"); return <div className="page"><div className="page-header" style={{display:"flex",justifyContent:"space-between"}}><div><h1>Community</h1><p>Exam-specific forums, study groups, and peer discussions.</p></div><button className="btn btn-primary" disabled>+ New Post</button></div><div style={{display:"grid",gridTemplateColumns:"200px 1fr 220px",gap:"1.25rem"}}><div className="card">Exam Spaces</div><div><div className="official-lock-banner mb-3">🔒 This channel contains admin-only official notifications. User posts are disabled in preview.</div>{THREADS.map((t)=><div key={t.id} className="card"><span className="tag tag-gray">{t.flair}</span><h3 className="font-semibold mt-2">{t.title}</h3><p className="text-sm text-gray-600">{t.preview}</p><p className="text-xs text-gray-400 mt-2">TODO: wire to production community threads tables.</p></div>)}</div><div className="card">Quota bar preview</div></div></div> }
